# Copie este arquivo para .env.local e ajuste os valores
#
# URL base da API (Spring Boot). Inclua o context-path /api.
# Em desenvolvimento local o backend sobe na porta 8090 (ver backend/src/main/resources/application.yml).
NEXT_PUBLIC_API_URL=http://localhost:8090/api
